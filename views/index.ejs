<%- include('header', {title: title}) %>

 <% if (locals.user) {%>
    <h1>Posts here:</h1>
    <% if(locals.posts) {%>
      <% locals.posts.forEach(post=>{ %>
      <div class="post">
        <p><%=post.title%></p>
        <p><%=post.body %></p>
      </div>
   <% }) %>
    
      <% } %>
  <% } else { %>
    <%- include('login') %>
<%}%>
    <%- include('footer') %>